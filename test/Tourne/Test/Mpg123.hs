-- | Tests for the Mpg123Encoding -> CInt mapping. The mapping function
-- is added as part of Fix #2; this test module lands with that fix.
module Tourne.Test.Mpg123 (tests) where

import Relude
import Test.Tasty (TestTree)

-- Empty for now; populated when fix #2 lands.
tests :: [TestTree]
tests = []
