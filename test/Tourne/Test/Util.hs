-- | Tests for shared utility helpers. The 'clamp' helper is dedup'd as
-- part of the structural cleanups; this test module lands with that fix.
module Tourne.Test.Util (tests) where

import Relude
import Test.Tasty (TestTree)

-- Empty for now; populated when 'clamp' is dedup'd.
tests :: [TestTree]
tests = []
